import { GuildMember } from "discord.js";
import type { Guild } from "discord.js";

export default async (member: GuildMember, guild: Guild | string) => {
	// if(typeof guild === Guild) {g}
	return;
};
